# Copy this to .env (do not commit .env)
NOTION_TOKEN=secret_abc123_from_notion_integration
# Use a PUBLIC Mapbox access token (never the secret); restrict it by domain in Mapbox dashboard.
MAPBOX_PUBLIC_TOKEN=pk.eyJ1IjoiEXAMPLE...your_public_token
# OpenAI API key for spanish-tips and recipes generation
OPENAI_API_KEY=sk-proj-your_openai_api_key_here
